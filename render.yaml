services:
  - type: web
    name: cv-format-api
    runtime: python
    region: singapore
<<<<<<< HEAD
    rootDir: backend
    buildCommand: apt-get update && apt-get install -y tesseract-ocr tesseract-ocr-vie tesseract-ocr-eng && pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port 8000
=======
    buildCommand: |
      set -eux
      apt-get update
      DEBIAN_FRONTEND=noninteractive apt-get install -y --no-install-recommends \
        tesseract-ocr \
        tesseract-ocr-vie \
        tesseract-ocr-eng
      tesseract --version
      cd backend
      python -m pip install --upgrade pip
      pip install --no-cache-dir -r requirements.txt
    startCommand: cd backend && uvicorn main:app --host 0.0.0.0 --port 8000
>>>>>>> 8d4af61 (fix deploy config and add template files)
    healthCheckPath: /health
    envVars:
      - key: JWT_SECRET
        sync: false
      - key: ANTHROPIC_API_KEY
        sync: false
      - key: PYTHON_VERSION
        value: "3.11.11"
      - key: GOOGLE_DRIVE_REFRESH_TOKEN
        sync: false
      - key: GOOGLE_DRIVE_CLIENT_ID
        sync: false
      - key: GOOGLE_DRIVE_CLIENT_SECRET
        sync: false
      - key: GOOGLE_DRIVE_FOLDER_ID
        sync: false
