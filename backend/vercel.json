{
  "name": "cv-format-web",
  "version": 1,
  "scripts": {
    "dev": "cd frontend && npm install && npm run dev",
    "build": "cd frontend && npm install && npm run build",
    "start": "cd backend && pip install -r requirements.txt && uvicorn main:app --host 0.0.0.0 --port 8000"
  }
}
